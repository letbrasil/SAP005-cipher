const cipher = {
  /* 
  encode: function(offset, yourMsg) {
    
  },

  decode: function(offset, yourMsg) {
    
  }

            OU

  encode(offset, yourMsg) {

  }

  decode(offset, yourMsg) {

  }
  */
};

export default cipher;
